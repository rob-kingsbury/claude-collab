# Allow access to uploaded files (overrides root .htaccess deny)
Require all granted

# Prevent PHP execution in uploads directory
php_flag engine off
AddHandler default-handler .php .phtml .phar

# Prevent directory listing
Options -Indexes
